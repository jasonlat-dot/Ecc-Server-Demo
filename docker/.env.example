# ========================================
# ECC Server Demo 环境变量配置文件
# 复制此文件为 .env 并修改相应的值
# ========================================

# Spring配置
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8089

# Jasypt加密密码（重要：生产环境必须修改）
JASYPT_ENCRYPTOR_PASSWORD=lijiaqiang1024@wt1314520

# 数据库配置
DB_HOST=your_production_db_host
DB_PORT=3306
DB_NAME=your_production_db
DB_USERNAME=your_db_user
DB_PASSWORD=your_db_password

# Redis配置
REDIS_HOST=your_production_redis_host
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# RabbitMQ配置
RABBITMQ_HOST=your_production_rabbitmq_host
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=your_rabbitmq_user
RABBITMQ_PASSWORD=your_rabbitmq_password

# 邮件配置
EMAIL_HOST=smtp.gmail.com
EMAIL_USERNAME=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_PORT=587

# JVM参数
JAVA_OPTS=-Xms1g -Xmx2g -XX:+UseG1GC -XX:+PrintGCDetails -XX:+PrintGCTimeStamps

# ========================================
# 开发环境默认配置（docker-compose.yml使用）
# ========================================

# MySQL开发环境配置
MYSQL_ROOT_PASSWORD=your_password
MYSQL_DATABASE=test
MYSQL_USER=ecc_user
MYSQL_PASSWORD=ecc_password

# Redis开发环境配置
REDIS_DEV_PASSWORD=your_redis_password

# RabbitMQ开发环境配置
RABBITMQ_DEFAULT_USER=admin
RABBITMQ_DEFAULT_PASS=admin123